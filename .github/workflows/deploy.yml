name: Deploy Frontend
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker login -u ${{ secrets.saransh138}} -p ${{ secrets.Himalayan2001@ }}
      - run: docker build -t saransh/react-test -f Dockerfile.dev .
      - run: docker run -e CI=true saransh/react-test npm test
